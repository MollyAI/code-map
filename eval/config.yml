# 外部仓库评测清单。每个 repo 钉死 commit SHA 保证可复现。
# 用法见 eval/README.md。
repos:
  - name: click
    url: https://github.com/pallets/click
    commit: c48021040a50c659b74e24ac2b11c9c9c6620a21
    build:
      skip: []
      focus: null
    expect:
      # src/click 本体 ~17 个文件;tests/examples 自 v1.4 起默认 skip,不再计入
      files_min: 15
      # template / sentinels / entry_points 可按实际跑出结果逐步补充
